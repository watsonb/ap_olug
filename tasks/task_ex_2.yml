---

- name: import/include tasks example 2 task 1
  debug:
    msg: "{{ inventory_hostname }} ex2 task 1"
    verbosity: "{{ pv | default(1) }}"

- name: import/include tasks example 2 task 2
  debug:
    msg: "{{ inventory_hostname }} ex2 task 2"
    verbosity: "{{ pv | default(1) }}"
